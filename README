This is a sample project to make a AIagent that can present data from sql/csv files. Without knowing the content from them.
It will have 5 sample lines and the column names.

Create a json file named settings.json with the following content:
{
  "genai_api_key": "your-api-key-here"
}